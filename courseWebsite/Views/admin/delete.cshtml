@model courseWebsite.Models.category

@{
    ViewBag.Title = "delete";
    Layout = "~/Views/Shared/_mainlayout.cshtml";
}

<html>
<body>
    <h3 class="alert alert-danger" style="text-align:center;">Are you sure you want to delete this category?</h3>
    <div>

        <hr />
        <dl class="dl-horizontal" style="font-size:x-large">
            <dt>
                @Html.DisplayNameFor(model => model.name):
            </dt>

            <dd>
                @Html.DisplayFor(model => model.name)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.parent_id):
            </dt>

            <dd>
                @Html.DisplayFor(model => model.parent_id)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color" style="text-align:center;padding-bottom:40px">
                <input type="submit" value="Delete" class="btn btn-primary" />

            </div>
        }
    </div>
    @if (ViewBag.massege1 != null)
    {
        <script type="text/javascript">

            $(document).ready(function () {
                
                    window.alert =  function () {
                     alert("@ViewBag.massege1");
                     };
                
            });
        @*window.alert =  $(function () {
            alert("@ViewBag.massege1");
            });*@
        </script>
    }
  
</body>
</html>